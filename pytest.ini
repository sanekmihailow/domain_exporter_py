[pytest]
asyncio_mode = auto
testpaths = tests
# Skip live-network tests by default; run them with `pytest -m integration`.
addopts = -m "not integration"
markers =
    integration: hits real RDAP/WHOIS upstreams over the network (slow, flaky)
